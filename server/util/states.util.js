const GOT_STATE = 'got';
const MISSING_STATE = 'missing';
const REPEATED_STATE = 'repeated';

const GOT_STATE_ID = 1;
const MISSING_STATE_ID = 2;
const REPEATED_STATE_ID = 3;

module.exports = {
    GOT_STATE,
    MISSING_STATE,
    REPEATED_STATE,
    GOT_STATE_ID,
    MISSING_STATE_ID,
    REPEATED_STATE_ID
}